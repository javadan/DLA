ul.button-group.round
  - Enrolment::ROLES.each do |role|
    - role_selected = (role == enrolment.role)
    li
      = link_to enrolment_path(enrolment, enrolment: { role: role }), method: :put, data: { remote: true, disable_with: '...' }, class: "button small secondary #{'alert' if role_selected}" do
        = role
  li
    = link_to icon(:x), enrolment, method: :delete, data: { remote: true, disable_with: '...' }, class: "button small secondary"